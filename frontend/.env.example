# Base URL of the SalesMotion API.
# Optional: services/api.ts falls back to http://localhost:5000/api when unset,
# so this only needs setting when the backend lives somewhere else.
REACT_APP_API_URL=http://localhost:5000/api
